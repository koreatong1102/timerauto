# Console Safe Hotfix

- 해설/캐스터 베스트 로직은 유지했습니다.
- 시작 직후 Edge TTS 선캐시가 실패할 때 콘솔이 대량 경고로 도배되지 않도록, 연속 실패 시 선캐시를 중단하게 했습니다.
- 선수 이름 조회가 `ocr.allow_chars` 누락 같은 설정 차이로 터지지 않도록 안전 처리했습니다.
- 일반 이벤트/브라우저/설정 UI 로직은 건드리지 않았습니다.
