# Stage54 Blackbox Visible Settings Hotfix

- Moved the SpectatorLog blackbox controls out of the crowded SpectatorLog integration grid.
- Added a clearly named top-level group in the Logs tab: `관전툴 로그 전체 기록 / 블랙박스`.
- The checkbox is now labeled `SpectatorLog 폴더 전체 기록 켜기` and is visually emphasized.
- Kept the default OFF behavior from Stage53.
- Kept the package clean: no generated `SpectatorLogArchive`, `logs`, or `diagnostics` session files are included.
